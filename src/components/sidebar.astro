---
import "../style/sidebar.css";

const pathname = Astro.url.pathname;
const isActive = (href: string) =>
  href === "/" ? pathname === href : pathname.startsWith(href);

const sections = [
  {
    id: "web-tool",
    label: "Web Tool",
    links: [
      { href: "/project/moitools", label: "MoITools" },
      { href: "/project/cyaks", label: "CyAks" },
    ],
  },
  {
    id: "software",
    label: "Software",
    links: [
      { href: "/project/mio", label: "mio-proxy" },
      { href: "/project/mcry", label: "Mcry" },
      { href: "/project/qcsh", label: "QuickShell" },
      { href: "/project/mineradio", label: "Mineradio" },
      { href: "/project/fancyindex-theme", label: "FancyIndex Theme" },
      { href: "/project/contento24", label: "Contento24" },
    ],
  },
  {
    id: "luci",
    label: "LuCI-APP",
    links: [
      { href: "/project/luci-app-ezsgx", label: "Easy sing-box" },
      { href: "/project/luci-app-openlist", label: "OpenList" },
      { href: "/project/luci-app-rsop", label: "Rustdesk for OpenWrt" },
      { href: "/project/luci-app-pm", label: "PowerManager" },
    ],
  },
  {
    id: "document",
    label: "Document",
    links: [
      { href: "/doc/OpenWrt", label: "OpenWrt" },
      { href: "/doc/Mirror", label: "Mirror" },
      { href: "/doc/MoPL", label: "Mo Public License" },
      { href: "/doc/bts", label: "Build This Site" },
    ],
  },
];
---

<button
  class="nav-trigger"
  type="button"
  aria-label="Open navigation"
  aria-controls="site-navigation"
  aria-expanded="false"
>
  <span></span><span></span><span></span>
</button>

<aside class="sidebar" id="site-navigation" aria-label="Site navigation">
  <div class="sidebar-header">
    <a class="brand" href="/" aria-label="Mo AEIOU home">
      <img src="/favicon.svg" alt="Logo" />
      <span>Mo AEIOU</span>
    </a>
    <button
      class="nav-close"
      type="button"
      aria-label="Close navigation"
      data-nav-close
    >
      <span></span><span></span>
    </button>
  </div>

  <div class="sidebar-content">
    {
      sections.map((section) => (
        <nav class="section" aria-labelledby={`nav-${section.id}`}>
          <h2 class="section-label" id={`nav-${section.id}`}>
            {section.label}
          </h2>
          <div class="nav-list">
            {section.links.map((link) => (
              <a
                href={link.href}
                class:list={["nav-item", { active: isActive(link.href) }]}
                aria-current={isActive(link.href) ? "page" : undefined}
              >
                {link.label}
              </a>
            ))}
          </div>
        </nav>
      ))
    }
  </div>

  <p class="sidebar-footer">&copy; 2026 MoAEIOU</p>
</aside>

<script>
  const sidebar = document.querySelector<HTMLElement>(".sidebar");
  const trigger = document.querySelector<HTMLButtonElement>(".nav-trigger");
  const closeButtons =
    document.querySelectorAll<HTMLElement>("[data-nav-close]");

  const setOpen = (open: boolean) => {
    document.documentElement.classList.toggle("nav-open", open);
    trigger?.setAttribute("aria-expanded", String(open));
    if (open) sidebar?.querySelector<HTMLElement>("a, button")?.focus();
    else trigger?.focus();
  };

  trigger?.addEventListener("click", () => setOpen(true));
  closeButtons.forEach((button) =>
    button.addEventListener("click", () => setOpen(false)),
  );
  sidebar?.querySelectorAll("a").forEach((link) =>
    link.addEventListener("click", () => {
      if (matchMedia("(max-width: 640px)").matches) setOpen(false);
    }),
  );
  document.addEventListener("keydown", (event) => {
    if (
      event.key === "Escape" &&
      document.documentElement.classList.contains("nav-open")
    ) {
      setOpen(false);
    }
  });
</script>
